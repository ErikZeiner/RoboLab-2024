// ENTER YOUR GEMINI API KEY HERE - IT IS NOT POSSIBLE TO USE GEMINI API FROM WITHIN THE EU WITHOUT A VPN
// This part of the app needs to be improved security-wise. Due to lack of more time for a proper implementation, a temporary API key, which is no longer functional, was used.
export let config = {
  KEY: '',
}
